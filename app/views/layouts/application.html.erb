<!DOCTYPE html>
<html>
<head>
  <title>Splurty</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
</head>
<body>
  <header>
    <div class="container">
      <div class="col-xs-3">
        <%= link_to root_path, class: 'logo' do %>
          <div class="circle">
            <br />
            Just Run
            <br />
            Yo Damn
            <br />
            Gym Brah
          </div>
        <% end %>
      </div>
      <div class="col-sm-4 hidden-sm-down slogan">
        Splurting out unsolicited advice about running gyms since 1956
      </div>
      <div class="col-xs-8 col-sm-5 col-xs-offset-1 col-sm-offset-0 links text-right">
        <!-- Button trigger modal -->
        <%= link_to 'Contribute', '#', data: {toggle: 'modal', target: '#myModal'} %>
        <%= link_to 'About', about_path %>
      </div>
    </div>
  </header>

  <!-- Modal -->
  <div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
          <h4 class="modal-title" id="myModalLabel">Contribute Your Quote</h4>
        </div>
        <%= simple_form_for Quote.new do |f| %>
          <div class="modal-body">
            <%= f.input :saying, label: "Enter your quote:", placeholder: "What is the quote that you'd like to add?" %><br />
            <%= f.input :author, label: "Enter the author:", placeholder: "Who claims such an epic utterance?" %><br />
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
            <%= f.submit 'Create', class: 'btn btn-primary' %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
  <!-- End Modal -->

  <hr />
  <br class="clear" />

  <div class="container">
    <% if flash[:error].present? %>
      <div class="col-xs-10 col-xs-offset-1 alert alert-danger">
        <%= flash[:error].html_safe %>
      </div><br class="clear" />
    <% end %>
    <%= yield %>
  </div>

  <footer class="footer">
    <div class="container">
      <div class="col-sm-12 col-md-6 text-white text-xs-center text-md-left align-middle">
        <%= link_to "This isn't enough, I need more", root_path, class: 'next-quote' %>
      </div>
      <div class="col-sm-12 col-md-6 text-white text-xs-center text-md-right footer-blurb align-middle">
        Proudly hacked in Virginia by Matt Verlaque<br />
      </div>
    </div>
  </footer>

</body>
</html>
